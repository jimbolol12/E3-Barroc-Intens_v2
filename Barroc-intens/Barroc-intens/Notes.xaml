<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Barroc_intens.Notes"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Barroc_intens"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0"
          Background="#FDD716">

            <Button HorizontalAlignment="Left"
                x:Name="BRedirectBack"
                Click="BRedirectBack_Click"
                Content="Terug"
                Margin="10,20,30,20"
                Background="black" />
        </Grid>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" /> 
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <TextBox x:Name="titel" PlaceholderText="titel"></TextBox>
                <TextBox x:Name="beschrijving" PlaceholderText=" beschrijving "></TextBox>
                <Button Click="Button_Click" >oplsaan</Button>
            </StackPanel>


        </Grid>
    </Grid>
</Window>
